<%# app/views/revenues/new.html.erb (adapter le titre pour Edit) %>
<% content_for :title, "Nouveau Revenu" %>

<div class="max-w-2xl mx-auto">
  <div class="text-sm breadcrumbs mb-6">
    <ul>
      <li><%= link_to "Revenus", revenues_path, class: "text-success" %></li>
      <li>Ajouter une entrée</li>
    </ul>
  </div>

  <div class="card bg-base-100 shadow-xl border border-base-200">
    <div class="card-body">
      <div class="flex items-center gap-3 mb-6">
        <div class="p-3 bg-success/10 text-success rounded-lg">
          <%= lucide_icon("trending-up", class: "size-6") %>
        </div>
        <h2 class="card-title text-2xl font-bold">Enregistrer un revenu</h2>
      </div>

      <%= render "form", revenue: @revenue %>
    </div>
  </div>
</div>